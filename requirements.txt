# Core numeric stack (works on Python 3.12)
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.2

# ONNX runtime (CPU). If you have CUDA set up, swap for onnxruntime-gpu==1.18.0
onnxruntime==1.18.0

# I/O + audio + utils
PyYAML==6.0.2
requests==2.32.5
sounddevice==0.4.7
webrtcvad==2.0.10

# TTS
piper-tts[http]

# OpenWakeWord
openwakeword